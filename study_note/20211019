sudo apt-get update : 리눅스에서 버전 관리 매니저
sudo apt-get -y upgrade : 버전 관리 매니저 업그레이드
sudo apt-get install build-essential : 파이썬이나 C++ 같은 build에서 핵심적인 기능을 담당하는 kit
sudo apt-get install curl : 웹사이트에서 접속해서 kit를 다운받을 수 있다.
curl -sL https://deb.nodesource.com/setup_14.x | sudo -E bash -- : 
sudo apt-get install -y nodejs : nodejs 설치
sudo apt-get install git : git 설치
sudo apt-get install vim : shell에서 code editor
touch .gitconfig : git에 대한 setting 파일 생성